package lab05;

//import static org.junit.jupiter.api.Assertions.*;

public class CircleTest { /**
    CircleTest(){
    }

    @Test
    void CircleTesting() {
        Circle myCircle = new Circle(100);
        Circle myCircle1 = new Circle(110);
        Assertions.assertEquals(myCircle.getArea(), (100*100*Math.PI));
        Assertions.assertEquals(myCircle1.getArea(), (110*110*Math.PI));

        // for instance myCircle
        myCircle.setRaduis(22); // changing the radius to 22
        myCircle.getArea(); // prompting the getArea to calculate new area
        Assertions.assertEquals(myCircle.getArea(), (22*22*Math.PI)); // asserting if they are equal

        // for instance myCircle1
        myCircle1.setRaduis(33); // changing the radius to 33
        myCircle1.getArea(); // prompting the getArea to calculate new area
        Assertions.assertEquals(myCircle1.getArea(), (33*33*Math.PI));  // asserting if they are equal
    }**/

}